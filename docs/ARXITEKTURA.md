# Система контроля объектов — архитектура

Рабочая система отдела проблемных активов «Микрокредитбанка». Предмет учёта —
физический объект (здание, помещение, участок, транспорт), который банк держит
под контролем: от начала взыскания и приёма на баланс до снятия с контроля и
архива. **85 экранов, 9 разделов, 6 ролей**, статический клиент без сборщика.

---

## 1. Технические слои

### 1.1 Клиент

| Файл | Назначение |
|---|---|
| `yadro/app.css` | Дизайн-система: токены, компоненты, состояния. Шрифты Outfit + Manrope |
| `yadro/app.js` | Оболочка: рельс, шапка, доступ по ролям, UZ/RU, движки таблиц и форм |
| `yadro/api.js` | Служба данных: есть сервер — REST, нет — локальный слой поверх браузера |
| `yadro/daraxt.js` | Два индекса: `MKB_DARAXT` — пункты рельса по разделам, `MKB_ICHKI` — внутренние страницы раздела, которых в рельсе нет |
| `yadro/ikonlar.js` | Спрайт иконок: 86 символов (Solar, CC BY 4.0) |
| `yadro/qollanma.js` | Порядок работы для каждой из шести ролей |
| `yadro/bino.js` | Геометрическая модель здания |
| `yadro/qavat.js` | Поэтажный план — архитектурный чертёж в SVG |
| `yadro/nav3d.js` | Трёхмерный навигатор по зданию на canvas |
| `yadro/chizma.js` | Графики: область, столбцы, сгруппированные столбцы, кольцо, спарклайн |
| `yadro/taqdimot-moslama.js` | Данные и расчёты презентации проекта; подключается только экраном `taqdimot.html` |
| `malumot*.js` | Слой данных — пять файлов со строгим порядком загрузки (раздел 3) |
| `tarjima.js` | Словарь UZ→RU: 3060 ключей и 89 правил; ключ — сам текст интерфейса |
| `assets/` | Шрифты в woff2, изображения, локальная копия картографической библиотеки |
| `vositalar/import_taqdimot.py` | Загрузка локального реестра банка из презентации (раздел 10); в работе клиента не участвует |

### 1.2 Общий каркас страницы

Экраны — плоский список HTML-файлов в корне репозитория, без вложенных папок и
без маршрутизатора: переход между экранами — обычная ссылка. У всех страниц
одинаковый пролог и одинаковый скелет разметки:

```
<link rel="stylesheet" href="yadro/app.css?v=…">
<body data-sahifa="<ключ раздела>">
  <div class="qobiq">
    <nav class="yon" id="yon"></nav>                  ← рельс рисует app.js
    <div class="sahna">
      <header class="shapka" id="shapka"></header>    ← шапку рисует app.js
      <main class="kontent">  … разметка экрана …
```

Порядок подключения одинаков на всех 85 страницах и является требованием, а не
стилем: каждый следующий файл данных дописывает результат предыдущего.

```
yadro/bino.js → malumot.js → malumot-qoshimcha.js → malumot-kengaytma.js →
malumot-kirish.js → malumot-indeks.js → tarjima.js → yadro/ikonlar.js →
yadro/daraxt.js → yadro/chizma.js → yadro/qollanma.js → yadro/api.js → yadro/app.js
```

`yadro/qavat.js` и `yadro/nav3d.js` подключают только два экрана — `obyekt-qavat.html`
и `obyekt-3d.html`. Остальным они не нужны и не грузятся. Точно так же
`yadro/taqdimot-moslama.js` подключает один экран — `taqdimot.html`, последним в
цепочке.

Семь страниц работают вне оболочки и помечены `data-ochiq="1"` — вход,
восстановление и смена пароля, две страницы ошибок, точка входа `index.html` и
презентация проекта `taqdimot.html`. Для них `app.js` пропускает проверку прав и
не рисует рельс с шапкой.

### 1.3 Сборка экранов

Страницы не правятся по одной: пролог, скелет разметки, порядок скриптов и метка
версии выставляются сборкой одинаково для всего набора. Отсюда два следствия,
видимых прямо в файлах:

- все ссылки на общие файлы несут одну и ту же метку версии (`?v=26091802`),
  поэтому кэш сбрасывается разом по всей системе, а не по одному экрану;
- индекс разделов `yadro/daraxt.js` и реестр экранов `docs/EKRANLAR.md`
  описывают один и тот же набор страниц: каждая страница оболочки числится либо
  в `MKB_DARAXT` (пункт рельса), либо в `MKB_ICHKI` (внутренняя страница
  раздела), и её `data-sahifa` совпадает с ключом этого раздела.

Оба следствия держатся не на дисциплине, а на прогоне
`tests/sahifalar.test.js` (раздел 11): пункт, ведущий на несуществующий файл,
страница с чужим ключом раздела или вторая метка версии — отказ проверки.

### 1.4 Сервер

`server/server.js` — чистый Node.js, без внешних пакетов:

- отдаёт статику из корня и обслуживает REST `/api/*`;
- при первом запуске засевает хранилища `server/malumotlar/*.json` — 26 коллекций,
  прочитанных из модулей данных в изолированном контексте;
- держит токены сессий в памяти, проверяет сессию на каждом запросе;
- до чтения из хранилища проверяет право роли на раздел, которому принадлежит
  коллекция: `KOLLEKSIYA_BOLIM` сопоставляет коллекцию с ключом раздела,
  `ROL_BOLIMLAR` повторяет клиентскую матрицу `ROL_RUXSAT`;
- пишет журнал действий `amallar.json`, хранит последние 2000 записей.

```
node server/server.js        →  http://localhost:8790
```

Порт переопределяется переменной `PORT`. На статическом хостинге сервера нет —
`api.js` сам переходит в локальный режим, изменения остаются в браузере.

---

## 2. Оболочка `yadro/app.js`

Единственный файл, который знает про разделы, роли и язык. Он владеет:

- **реестром разделов** `BOLIMLAR` — 8 записей: ключ, ярлык, иконка, входная
  страница. Порядок в массиве и есть порядок рельса; девятый раздел — настройки
  (`sozlama`) — рисуется отдельной группой внизу рельса;
- **матрицей доступа** `ROL_KALIT`, `ROL_RUXSAT`, `SAHIFA_MAXSUS` (раздел 6);
- **рельсом** `yonChiz()`: группы разворачиваются из `MKB_DARAXT` — в группе от
  трёх до семи пунктов, остальные страницы раздела достижимы с ленты вкладок и
  из карточек; пункт без права не рисуется вовсе; внизу — настройки, ссылка на
  презентацию проекта, индикатор режима данных и выход;
- **соответствием «файл → раздел»** `FAYL_BOLIM`: собирается из `MKB_DARAXT` и
  `MKB_ICHKI`, поэтому право проверяется для любой ссылки, в том числе на
  внутреннюю страницу, которой нет в рельсе (раздел 6);
- **лентой вкладок раздела** `nav.bolim-tablar`: состав ленты задан в разметке
  и одинаков на всех страницах раздела, оболочка убирает из неё вкладки,
  закрытые текущей роли;
- **шапкой** `shapkaChiz()`: быстрые пилюли по первым четырём доступным разделам,
  поиск по реестру (`/` — фокус, Enter — переход), переключатель UZ/RU, кнопка
  порядка работы, уведомления, меню профиля, мобильное меню;
- **переводом** `tarjimaQil()`: обход текстовых узлов плюс шесть атрибутов из
  списка `TARJIMA_ATTR` — подсказки полей ввода, всплывающие подписи,
  `aria-label`, `alt` и тексты уведомлений на кнопках.
  Исходный текст запоминается в `WeakMap`, поэтому возврат на узбекский точный.
  Кроме словаря работает список регулярных правил. Прогон повторяется через
  250 и 1000 мс — для содержимого, дорисованного скриптом экрана;
- **движками интерфейса** `MKB.*`: `jadval` (поиск, фильтры, сортировка,
  постраничность, пустое состояние), `forma` (сбор значений и подсветка
  незаполненных обязательных полей), `modal`, `yonPanel`, `toast`, `zanjir`
  (цепочка этапов), `halqa`, `shtrix`, `barlar` (полосы распределения),
  форматирование сумм и чисел.

Точка входа — `DOMContentLoaded`: вставить спрайт, проверить сессию и право на
страницу, нарисовать рельс и шапку, запустить перевод. Без сессии — переход на
`kirish.html`, без права — на `xato-403.html`.

Кнопка «как работать в системе» в шапке открывает боковую панель с порядком
работы для текущей роли из `yadro/qollanma.js`: цель, дневной порядок шагов со
ссылками, зона ответственности, сроки и перечень закрытых разделов. Полная
версия — экран `qollanma.html`.

---

## 3. Слой данных — порядок загрузки

Пять файлов, каждый дописывает `window.MKB_DATA`. Порядок обязателен.

| # | Файл | Что добавляет |
|---|---|---|
| 0 | `yadro/bino.js` | Модель здания. Публикует `MKBbino`, не трогает `MKB_DATA`, но нужен следующим |
| 1 | `malumot.js` | Ядро: этапы, записи объектов, индекс объектов, осмотры, полисы, оценки, документы, происшествия, задачи, уведомления, сотрудники, регионы, точки карты, форматирование сумм, `moslikTekshiruvi()` |
| 2 | `malumot-qoshimcha.js` | Производные коллекции: судебные заседания, адвокаты, расходы, аренда, реструктуризация — все выводятся из записей, без вторых копий; расширяет `moslikTekshiruvi()` |
| 3 | `malumot-kengaytma.js` | Доводит охват до полного реестра: достраивает записи, осмотры, полисы, оценки, документы, происшествия, задачи и документы, затем пересчитывает сводные показатели и срез по регионам |
| 4 | `malumot-kirish.js` | Модуль контроля доступа: лица, точки доступа (берутся из модели здания), устройства, разрешения, заявки, визиты, события, инциденты, дистанционные сессии, сервисные работы, сводка `kirishJamlama()` |
| 5 | `malumot-indeks.js` | Индекс контроля объекта (раздел 4) |

Объём после полной загрузки: 64 объекта, 187 осмотров, 59 полисов, 88 оценок,
222 документа, 20 архивных записей; по контролю доступа — 508 точек, 1765
устройств, 40 лиц, 169 разрешений, 46 заявок, 128 визитов, 720 событий,
64 инцидента, 28 дистанционных сессий, 52 сервисные работы.

Два правила держат слой связным:

1. **Один источник.** Экраны не хранят собственных массивов: сумма, дата и
   состояние по объекту одинаковы во всех разделах, потому что читаются из
   одного места.
2. **Воспроизводимость.** Достраивание данных детерминировано — фиксированное
   зерно псевдослучайности в каждом файле, системная дата закреплена на
   26 августа 2026 г. Один и тот же объект всегда даёт одни и те же цифры.

`moslikTekshiruvi()` — набор инвариантов поверх загруженных данных: сходимость
сумм, целостность ссылок между коллекциями, соответствие состояния объекта его
этапу. На текущих данных возвращает пустой список нарушений.

---

## 4. Индекс контроля объекта

Центральный показатель системы, 0–100 %. Он не хранится в записи, а
пересчитывается из текущих данных при каждой загрузке — поэтому не может
разойтись с реальностью. Складывается из пяти проверок:

| Проверка | Вес | Норма |
|---|---|---|
| Актуальность осмотра | 25 | каждые 90 дней; транспорт — 30, участки — 180 |
| Страховая защита | 25 | полис действует, покрытие не ниже оценки |
| Актуальность оценки | 20 | раз в 12 месяцев |
| Полнота документов | 15 | три обязательных документа |
| Контроль доступа | 15 | все точки объекта на связи, нет незакрытых инцидентов |

Просрочка нормы снижает вклад ступенчато: в пределах нормы — полный вес, до
полутора норм — 0,6, до двух — 0,3, дальше — ноль. Уровни: **85 и выше** —
высокий контроль, **70** — устойчивый, **50** — требует внимания, ниже —
критический.

Каждая проверка возвращает не только долю, но и фактическое значение, норму и
причину отклонения. Поэтому карточка объекта показывает не голое число, а то,
чего именно не хватает: «полис истекает через 12 дней», «не хватает кадастрового
документа», «две точки доступа не на связи».

Публичные точки: `MKB_DATA.nazoratIndeksi(объект)` — разбор по проверкам,
`MKB_DATA.nazoratJamlama()` — среднее и распределение по уровням,
`объект.mulk.nazoratBall` — готовый балл для списков и таблиц.

Тот же файл публикует `MKB_DATA.BALANS_DINAMIKA` — ряд по годам (2021–2026):
сколько объектов принято на баланс и сколько реализовано. Из него строится
график блока «Имущество на балансе» главной панели (раздел 7).

---

## 5. Модель здания и движки отрисовки

### 5.1 `yadro/bino.js` — модель

Одна геометрическая модель на объект, построенная от его идентификатора и типа:
свёртка имени даёт зерно, зерно — детерминированный генератор, результат
кэшируется. Десять типологий (административное здание, торговый комплекс,
торговая площадь, склад, производство, жилой комплекс, квартира, жилой дом,
транспорт, участок) плюс запасная.

Единица измерения — метр, начало координат — левый верхний угол габарита.
Размеры: наружная стена 0,40 м, перегородка 0,15 м, коридор 2,40 м, дверь
0,95 м, окно 1,70 м, высота этажа 3,60 м. Шестнадцать типов помещений, четыре
состояния: в использовании, свободно, опечатано, в ремонте.

Модель отдаёт этажи, помещения с координатами и площадью, стены, дверные и
оконные проёмы, точки доступа, суммарную площадь и количество помещений.

Из неё читают **три** потребителя — план этажа, трёхмерный навигатор и реестр
точек доступа. Именно поэтому чертёж, объёмный вид и список точек не расходятся
между собой.

### 5.2 `yadro/qavat.js` — план этажа

Архитектурный чертёж в SVG, масштаб 1 м = 10 единиц: сетка осей с разметкой,
стены с толщиной, вырезанные в них проёмы, створки дверей с дугой открывания,
внешние рамы окон, штриховка опечатанных и ремонтируемых помещений, подписи с
площадью, значки точек доступа, размерные цепочки по двум сторонам, масштабная
линейка, стрелка севера и условные обозначения. Помещение выбирается щелчком и
с клавиатуры; щелчок по точке доступа даёт отдельное событие.

### 5.3 `yadro/nav3d.js` — трёхмерный навигатор

Собственный движок поверх canvas 2D, без внешних библиотек: перспективная
камера, орбита мышью и колесом, сортировка граней по глубине, затенение по
направлению света, фасады с оконными проёмами, цоколь, плиты перекрытий,
площадка и дорожки. Три режима просмотра: здание целиком, внутренний срез до
выбранного этажа, разнесённые этажи. Помещение выбирается попаданием точки в
многоугольник грани. Пересчёт размеров — по `ResizeObserver`.

### 5.4 `yadro/chizma.js` — графики

SVG без библиотек, цвета берутся из токенов CSS: сглаженная область
(Catmull-Rom → кривые Безье), столбцы с сеткой, сгруппированные столбцы
`ustunGuruh` (несколько рядов на одну подпись оси), кольцевая диаграмма с итогом
в центре, мини-спарклайн для карточек показателей. Общая подсказка при наведении.

Карта — единственная внешняя библиотека, лежит локально в `assets/vendor/` и
используется одним экраном — `xarita.html`.

---

## 6. Роли и доступ

Шесть ролей. Ключ роли вычисляется из названия должности в сессии, дальше всё
решает матрица.

| Роль | Ключ | Доступные разделы |
|---|---|---|
| Администратор | `admin` | все |
| Руководитель филиала | `filial` | все девять разделов |
| Менеджер объектов | `obyekt` | панель, реестр, контроль доступа, осмотры, отчёты, задачи, настройки |
| Инспектор осмотров | `nazorat` | панель, реестр, контроль доступа, осмотры, оценка и страхование, отчёты, задачи, настройки |
| Специалист-оценщик | `baholash` | панель, реестр, оценка и страхование, отчёты, задачи, настройки |
| Юрист | `yurist` | панель, взыскание и суд, реестр, отчёты, задачи, настройки |

Карта и архив входят в раздел реестра, поэтому открыты всем, кому открыт реестр.
Страховые полисы входят в раздел «Оценка и страхование» — именно поэтому этот
раздел открыт инспектору осмотров, который отвечает за страховую защиту.

Проверка двухслойная:

1. **Раздел.** Каждая страница несёт `<body data-sahifa="…">`; `ROL_RUXSAT`
   решает, доступен ли раздел. Недоступный раздел не появляется ни в рельсе, ни
   в пилюлях шапки, а прямой переход по адресу ведёт на `xato-403.html`.
   Для ссылок раздел определяется по имени файла через `FAYL_BOLIM`. Внутренние
   страницы (карточки, формы, шаги мастеров) сопоставлены разделам в
   `MKB_ICHKI`, поэтому проверка ссылки на них даёт тот же ответ, что и
   проверка самой страницы: среди экранов оболочки нет файла, раздел которого
   неизвестен и право на который считалось бы выданным по умолчанию.
2. **Отдельные файлы.** `SAHIFA_MAXSUS` сужает доступ к семнадцати экранам
   поверх раздела: шесть панелей, пользователи и карточка пользователя, матрица
   ролей, журнал действий, интеграции, филиалы, мастер установки устройства,
   три шага приёма на баланс и редактирование записи объекта.

Стартовая страница у каждой роли своя: `panel-<ключ>.html`, у администратора —
`panel.html`. Тот же список ролей повторён на сервере: запрос без сессии
возвращает 401, журнал действий не администратору — 403, коллекция закрытого
для роли раздела — 403. Клиент и сервер проверяют право независимо; совпадение
состава разделов в обеих матрицах проверяется автоматически.

---

## 7. Разделы и экраны

| Раздел | Ключ | Экранов |
|---|---|---|
| Панель управления | `panel` | 6 |
| Реестр объектов (с картой и архивом) | `aktivlar` | 17 |
| Контроль доступа | `kn` | 17 |
| Осмотры | `korik` | 6 |
| Оценка и страхование | `baholash` | 7 |
| Взыскание и суд | `yuridik` | 6 |
| Отчёты | `hisobot` | 8 |
| Задачи | `vazifa` | 3 |
| Настройки | `sozlama` | 8 |
| Вход, ошибки и презентация (вне рельса) | — | 7 |

Итого 85 экранов: 78 внутри оболочки и 7 открытых. Пофайловый реестр —
`docs/EKRANLAR.md`.

### Состав после укрупнения разделов

Разделов девять вместо прежних тринадцати. Ключи `arxiv`, `xarita`, `sugurta` и
`hujjat` упразднены и не встречаются ни в `data-sahifa`, ни в матрицах ролей:

- **карта** `xarita.html` и **архив** `arxiv.html`, `arxiv-obyekt.html` вошли в
  реестр объектов — это три представления одного и того же реестра;
- **страхование** `sugurta.html`, `sugurta-polis.html`, `sugurta-yangilash.html`
  вошло в раздел оценки, который теперь называется «Оценка и страхование»;
- **отчёт по контролю доступа** `kirish-hisoboti.html` перешёл в раздел отчётов;
- отдельного раздела документов нет: документы ведутся в карточке объекта
  (`obyekt-hujjatlar.html`);
- акт осмотра печатается прямо с листа `korik-akti.html` средствами браузера
  (`window.print()`), отдельной печатной страницы нет.

Тридцать два экрана выведены из состава продукта. Ссылок на них не осталось —
ни в рельсе, ни в лентах вкладок, ни в тексте страниц; их файлы подлежат
удалению из репозитория и в `docs/EKRANLAR.md` не числятся.

### Ленты вкладок разделов

У каждого раздела одна каноническая лента `nav.bolim-tablar`, одинаковая на всех
его страницах:

| Раздел | Вкладки |
|---|---|
| Реестр объектов | реестр · на карте · архив |
| Контроль доступа | пост · точки · события · визиты · разрешения · устройства · инциденты |
| Осмотры | план · просроченные · история |
| Оценка и страхование | оценка · заказ · ввод отчёта · страховые полисы · продление полиса |
| Взыскание и суд | доска взыскания · календарь суда · подготовка иска |
| Отчёты | центр · портфель · взыскание · регионы · расходы · KPI · контроль доступа |
| Задачи | задачи · утверждения · уведомления |
| Настройки | профиль · пользователи · роли · филиалы · интеграции · журнал действий |

У карточки объекта собственная лента из восьми вкладок: общие сведения, финансы,
документы, осмотры, расходы, 3D-навигатор, план этажа, история. Вкладка ленты
обязана вести на страницу того же раздела — это проверяется автоматически.

### Главная панель: блок «Имущество на балансе»

`panel.html` помимо объекта в фокусе, показателей взыскания и воронки содержит
блок «Balansdagi mulk»:

- стоимость имущества на балансе и число объектов;
- график по годам «принято на баланс / реализовано» — `MKBchizma.ustunGuruh`
  на данных `MKB_DATA.BALANS_DINAMIKA`;
- разрез по регионам;
- «Охват контролем» — четыре кольца: осмотр, страхование, оценка, контроль
  доступа; каждое кольцо — ссылка в соответствующий раздел.

### Презентация проекта

`taqdimot.html` — интерактивная презентация проекта для руководства, открытая
страница (`data-ochiq="1"`): ссылка на неё стоит внизу рельса и на главной
панели. Десять сцен: обложка, текущее положение, решение, объект без
электроснабжения (варианты решения), расчёт энергии (калькулятор), распознавание
лица и пожарная защита, смета (уровни защиты и калькулятор сметы с выгрузкой в
CSV), экраны системы, план внедрения, решение.

Цифры презентации берутся из того же `MKB_DATA` через адаптер
`yadro/taqdimot-moslama.js`, поэтому совпадают с главной панелью. Введённые в
калькуляторы значения хранятся только в браузере — ключ `localStorage`
`mkb-taqdimot`. Параметр `?sahna=N` открывает презентацию с сцены N, `?faqat=N`
показывает одну сцену отдельно.

### Контроль доступа — самый крупный раздел

Семнадцать экранов, закрывающих электронный контроль физического доступа на
объект:

- **пост оператора** `kirish-nazorati.html` — очередь заявок, активные визиты,
  поток событий и состояние точек в одном месте;
- **реестры** — точки доступа и карточка точки, устройства и карточка устройства,
  разрешения, визиты;
- **журнал событий** и карточка события с решением оператора;
- **заявка на доступ** и очередь заявок;
- **живой визит** `tashrif-jonli.html` — подтверждение личности, таймер
  пребывания, управление дверью;
- **проверка на выходе** `tashrif-chiqish.html` — сверка по описи;
- **дистанционный осмотр** `masofaviy-korik.html` — сессия по шагам;
- **мастер установки устройства** `qurilma-ornatish.html` — четыре шага:
  размещение, устройство, подключение, активация; шесть типов оборудования;
- **сервисное задание** и **доска инцидентов** с карточкой инцидента, которая
  закрывается заключением и подтверждающим материалом.

Отчёт за период `kirish-hisoboti.html` относится к разделу отчётов.

---

## 8. Жизненный цикл объекта

```
Просрочка по договору
  └─ Предупреждение → Исковое заявление → Судебное производство → Решение суда
      └─ Исполнение решения → Изъятие
          └─ Приём на баланс: акт, комиссия, документы
              └─ Хранение и контроль: осмотр · страхование · переоценка ·
                 контроль доступа · учёт расходов
                  └─ Снятие с контроля → Архив
```

Объект идёт по цепочке под одним номером вида `AK-YYYY/NNNN`. Этапы строго
последовательны, перескок через этап невозможен. Состояние объекта не задаётся
руками — оно выводится из этапа: «в обеспечении», «в процессе изъятия»,
«изъято», «хранится на балансе».

Продажа объекта в границы системы не входит: последний этап цепочки — **«На
балансе»**. Дальнейшее движение имущества ведётся вне этого контура.

---

## 9. Два режима данных

`yadro/api.js` — единственная точка доступа к данным. Экраны не вызывают `fetch`
напрямую, поэтому смена режима не требует правки ни одного экрана.

При загрузке модуль запрашивает `/api/salomat` с таймаутом 700 мс. Ответ — режим
`server`, отсутствие ответа — режим `snapshot`. Выбранный режим публикуется
событием `mkb:rejim`, оболочка показывает индикатор внизу рельса.

| Операция | Режим `server` | Режим `snapshot` |
|---|---|---|
| `royxat(коллекция, фильтр)` | `GET /api/<коллекция>` | массив из `MKB_DATA` плюс слой правок из браузера |
| `bitta(коллекция, id)` | `GET /api/<коллекция>/<id>` | поиск по тому же массиву |
| `yangilash(коллекция, id, поля)` | `PATCH` | правка ложится в слой, исходные данные не трогаются |
| `yangi(коллекция, объект)` | `POST` | новая запись добавляется в слой |
| `kirish` / `chiqish` / `sessiya` | токен сервера | сессия в браузере по выбранной роли |

В локальном режиме правки хранятся отдельным слоем поверх исходных данных:
запись помечается как изменённая, созданная или удалённая, а базовый массив
остаётся нетронутым. Каждое изменение попадает в журнал действий.

Контракт `server/server.js`:

```
GET   /api/salomat              доступность, без сессии
POST  /api/kirish               {login, parol, rol} → {token, ism, rol, filial}
GET   /api/<коллекция>          список; ?поле=значение — фильтр, ?q= — поиск по записи
GET   /api/<коллекция>/<id>     одна запись
POST  /api/<коллекция>          создание, идентификатор выдаётся при отсутствии
PATCH /api/<коллекция>/<id>     обновление полей
GET   /api/amallar              журнал действий, только администратор
```

Коды ответов: 401 — нет сессии, 403 — журнал запрошен не администратором,
404 — нет такой коллекции или записи, 405 — метод не поддержан. Полный контракт
серверной части — `docs/API.md`.

---

## 10. Источник реестра: демонстрационные данные и локальный реестр

### 10.1 Демонстрационные данные

По умолчанию система работает на условных демонстрационных данных, приведённых
к практике банка: филиалы названы в форме БХМ/БХО (`Yunusobod BXM`,
`Samarqand BXO`), регионов четырнадцать (включая Сырдарьинскую область), их
количества в сумме дают итог портфеля `PORTFEL.jami` — 1248; названия типов
объектов и изображения согласованы между собой. Для объектов на этапе «На
балансе» интерфейс говорит языком баланса — «Balans qiymati», «Balansda N kun», —
а не языком задолженности и просрочки. Персональных и банковских сведений в
демонстрационных данных нет, только они попадают в репозиторий и на публичный
стенд.

### 10.2 Локальный реестр

Фактический перечень имущества на балансе банка — конфиденциальные сведения. Он
подключается только на рабочем компьютере и в репозиторий не попадает.

```
python vositalar/import_taqdimot.py "<путь к презентации>.pptx"
```

Сценарий разбирает слайды презентации «имущество на балансе» (Python 3.9+; для
фотографий нужен пакет Pillow, без него импорт проходит без фотографий) и пишет
два результата в каталог `mahalliy/` в корне проекта: `mahalliy/obyektlar.json` —
объекты и филиалы, `mahalliy/rasm/*.jpg` — уменьшенные фотографии со слайдов.

Чтение выполняет `malumot-kengaytma.js`:

- `mahalliyOqish()` запрашивает `mahalliy/obyektlar.json` только тогда, когда
  страница открыта с адреса `localhost`, `127.0.0.1` или `[::1]`. На любом другом
  адресе запрос не выполняется вообще;
- `mahalliyQolla()` накладывает сведения объекта — наименование, адрес, регион,
  филиал, прежнего владельца, балансовую стоимость, дату приёма на баланс, цену
  реализации, фотографию — на запись реестра. Идентификатор записи и все связи
  (дело, осмотры, полисы, точки доступа) не меняются, поэтому остальные модули
  и правила согласованности работают без правок;
- при локальном реестре итог портфеля и разрез по регионам считаются по
  фактическим объектам, стоимость на балансе — суммой балансовых стоимостей;
- `MKB_DATA.MANBA` сообщает выбранный источник: `mahalliy` или `shartli`.

Индикатор внизу рельса показывает «Mahalliy reyestr» вместо «Namoyish rejimi».
Щелчок по нему переключает на демонстрационные данные и обратно; выбор хранится
в `localStorage` под ключом `mkb-manba` (значение `shartli` — локальный реестр
не читать).

### 10.3 Гарантии конфиденциальности

1. Каталог `mahalliy/` внесён в `.gitignore`: ни реестр, ни фотографии не
   попадают в репозиторий, а значит и в публикуемую копию системы.
2. Данные читаются только с локального адреса. В опубликованной копии файла нет,
   и запрос к нему не делается — там всегда демонстрационные данные.
3. Файл читается браузером с того же компьютера; сценарий импорта и слой данных
   никуда его не передают.
4. Вне браузера локальный реестр не читается: засев хранилищ сервера и все
   автоматические проверки всегда идут на демонстрационных данных, поэтому их
   результаты воспроизводимы на любом компьютере.
5. Если файла нет, слой данных запоминает это до конца сеанса вкладки
   (`sessionStorage`, ключ `mkb-mahalliy-yoq`) и повторных запросов не делает.

Рекомендуемый способ локального просмотра — статический сервер на `localhost`
без сервера данных (режим `snapshot`). При поднятом `server/server.js`
индикатор показывает «Server ulangan», и переключение щелчком недоступно.

### 10.4 Как отключить

- щёлкнуть индикатор «Mahalliy reyestr» внизу рельса — система перейдёт на
  демонстрационные данные и останется на них до обратного щелчка;
- либо задать в консоли браузера `localStorage.setItem("mkb-manba", "shartli")`;
- либо удалить или переименовать каталог `mahalliy/` — это единственное место,
  где хранятся фактические данные.

Перед показом системы на чужом компьютере или с демонстрацией экрана вне банка
локальный реестр отключается.

---

## 11. Контроль качества

| Прогон | Проверок | Что закрывает |
|---|---|---|
| `node tests/malumot.test.js` | 44 | Инварианты ядра данных в восьми группах: границы расчётных функций, форматирование сумм, целостность ссылок реестра, сходимость производных значений, словарь перевода. Часть проверок ломает данные намеренно и требует, чтобы `moslikTekshiruvi()` это поймал с точной диагностикой |
| `node tests/nazorat.test.js` | 21 | Геометрия здания (помещения не выходят за габарит и не накладываются, у каждого есть выход в коридор, окна только в наружных стенах, модель воспроизводима), связность модуля контроля доступа, индекс контроля (сумма весов равна 100, границы 0–100, падение при нехватке данных), совпадение списка ролей во всех источниках и наличие стартовой страницы у каждой роли |
| `node tests/sahifalar.test.js` | 9 | Целостность навигации и страниц: каждый файл из `MKB_DARAXT` и `MKB_ICHKI` существует; ключи `BOLIMLAR` есть в дереве и разделов не больше девяти; `data-sahifa` каждой страницы совпадает с её разделом в дереве; все внутренние ссылки страниц, оболочки, службы данных и рабочего руководства ведут на существующие файлы; вкладки ленты раздела ведут на страницы того же раздела; метка версии одна на все страницы; в матрицах ролей клиента и сервера нет упразднённых ключей разделов |
| `node tests/server.test.js` | 16 | REST: подъём сервера, 401 без сессии, вход, отказ при неверном пароле и неизвестном логине, роль берётся из учётной записи, список и запись по идентификатору, сохранение правки на диск, журнал действий закрыт юристу и открыт администратору, коллекция закрытого раздела — 403, открытого — 200, 404 на неизвестную коллекцию |

Все четыре прогона требуют только Node, без внешних пакетов, и на текущих данных
проходят полностью: 90 проверок, ноль отказов.
