# Техническое задание
## Система контроля проблемных активов «Микрокредитбанк»

Версия 3.4 · Редакция от 25.09.2026

> С сентября 2026 года система ведёт имущество на балансе банка, а проблемный
> кредит — отдельным делом взыскания (`UNDIRUV_ISHLAR`). Модель актива, сроки,
> резерв, роли и состав экранов описаны в `docs/ARXITEKTURA.md`, контракт
> данных — в `docs/API.md`, реестр экранов — в `docs/EKRANLAR.md`. Там, где
> настоящее задание расходится с этими документами, действуют они.
> Трёхмерный навигатор, план этажа и изометрические чертежи объектов из системы
> выведены (раздел 5.10).
> В редакции 3.3 состав экранов сохранён полностью, а навигация и ролевая
> модель упрощены: шесть рабочих разделов вместо десяти, четыре рабочие роли
> и технический администратор вместо десяти ролей, один центр отчётов вместо
> девяти отчётных страниц в рельсе. Ни один экран не удалён (раздел 2 и 5).

---

## 1. Назначение и границы

### 1.1 Что автоматизирует система

Жизнь физического объекта — здания, помещения, земельного участка, транспортного
средства — под контролем банка: от правовой работы по взысканию до приёма на
баланс, последующего наблюдения и выбытия в архив.

```
Предупреждение → Исковое заявление → Судебное разбирательство → Решение суда →
Исполнение решения → Конфисковано → На балансе → Контроль → Архив
```

Семь этапов дела хранятся в одном месте (`BOSQICHLAR` в `malumot.js`) и проходятся
только по порядку, каждый переход попадает в историю дела. Статус объекта не
вводится вручную — он выводится из этапа:

| Этап дела | Статус объекта |
|---|---|
| Предупреждение · Исковое заявление · Судебное разбирательство | В обеспечении |
| Решение суда · Исполнение решения | В процессе конфискации |
| Конфисковано | Конфисковано |
| На балансе | Хранится на балансе |

После приёма на баланс объект переходит в режим постоянного наблюдения: осмотры,
страховая защита, актуальность оценки, комплектность документов и электронный
контроль доступа. Состояние наблюдения выражается одним числом — индексом
контроля (раздел 4).

### 1.2 Что система НЕ делает

- **Не ведёт кредитный конвейер.** Не начисляет проценты, не формирует проводки,
  не заменяет АБС. Сведения о договоре, клиенте и остатке задолженности приходят
  извне и используются только как основание правовой работы по объекту.
- **Не проводит торги и не принимает деньги.** Реализация ведётся в системе от
  начала до конца: лот и его публикация, предложения покупателей с проверкой AML
  и аффилированности, решение комиссии, договор купли-продажи, график платежей,
  аренда, снятие запрета и выбытие с баланса в архив. Сами торги идут на площадке
  E-auksion, платежи зачисляются в АБС. Пока интеграции не подключены (раздел 9),
  итог торгов и платёж вносит сотрудник вручную, и запись помечается как ручная.
- **Не подписывает документы электронной подписью.** Акты печатаются на бумаге
  из единого шаблона (раздел 5.12) с кодом проверки и QR; юридическую силу акту
  даёт подпись комиссии. Подпись E-IMZO — раздел 9.
- **Не заменяет физическую охрану.** Банк не выставляет постов; система
  фиксирует, кто, когда, на каком основании и через какую точку прошёл на объект,
  и в каком состоянии объект находится.
- **Не хранит производные величины.** Индекс контроля, статусы, итоги и проценты
  каждый раз пересчитываются из исходных записей.

### 1.3 Состав поставки

| Часть | Содержание |
|---|---|
| Клиентское приложение | 101 экран: 94 в оболочке (панель роли, шесть рабочих разделов и блок настроек) и 7 открытых страниц, единая оболочка `yadro/app.js`, оформление `yadro/app.css`, бизнес-операции `yadro/amal.js` |
| Модуль данных | `malumot.js` и его расширения — единственный источник значений |
| Сервер | `server/server.js` — REST-интерфейс, сессии, проверка роли на каждом запросе, запись изменений на диск |
| Фото объектов | вкладка «Suratlar» (`obyekt-suratlar.html`): фактические фото и схемы из локального реестра, загрузка через `MKBapi.fayl` |
| Графика | `yadro/chizma.js` — построение диаграмм без внешних библиотек |
| Рабочее руководство | `yadro/qollanma.js` и экран `qollanma.html` — порядок работы для каждой роли |
| Презентация проекта | `taqdimot.html` с собственными модулями `yadro/taqdimot*.js` — презентация для руководства (раздел 5.11) |
| Загрузка локального реестра | `vositalar/import_taqdimot.py` — перенос фактического перечня имущества на рабочий компьютер (раздел 7.1) |
| Печать и импорт | `yadro/dalolatnoma.js`, `yadro/dalolatnoma.css` — единый шаблон семи печатных форм с кодом проверки и QR; `yadro/jadval-fayl.js` — чтение `.xlsx`, шаблоны для импорта (раздел 5.12) |
| Автоматические проверки | наборы `tests/*.test.js` без внешних пакетов, в том числе `tests/hujjat.test.js` (печатные формы, QR, Excel, маршрут инспектора, четыре глаза при импорте); сценарии `tests/tarjima.mjs` и `tests/olcham.mjs` в Edge |
| Запуск на рабочем компьютере | `ishga-tushirish.cmd` — локальный сервер на 127.0.0.1:8777 и браузер |

Приложение работает в двух режимах через единую точку `yadro/api.js`: при
доступном сервере — REST с сохранением на диск, без сервера — снимок данных с
наложением изменений в браузере. Страницы никогда не обращаются к сети напрямую.

Состав 14 внешних систем, их состояние и условия подключения — раздел 9.2.
До подключения данные этих систем вносятся вручную, и запись помечается как ручная.

---

## 2. Роли и разграничение доступа

Роль определяется по зоне ответственности за объект, а не по названию
подразделения. Ролей физической охраны нет: банк не выставляет постов, он
контролирует наличие объекта, его состояние и доступ к нему.

Рабочих ролей четыре, пятая — технический администратор. Учётные записи
`Filial rahbari`, `Baholovchi`, `Baholovchi mutaxassis`,
`Realizatsiya mutaxassisi`, `Yurist`, `Xavfsizlik xizmati` и
`Ko'rik inspektori` сняты, их работа сохранена целиком и передана оставшимся
ролям.

| Роль | Ключ | Зона ответственности | Стартовая страница | Пунктов рельса |
|---|---|---|---|---|
| **Руководство** | `rahbariyat` | Председатель, член правления, управляющий филиалом: общая картина, утверждение решений, отчётность, распределение задач. Формы не заполняет | `panel.html` | 6 |
| **Менеджер объектов** | `obyekt` | Реестр, приём на баланс, полнота карточки и документов, заказ оценки, подготовка к продаже, лоты, договоры, аренда, дела о взыскании и суд, расходы на содержание | `panel-obyekt.html` | 6 |
| **Инспектор осмотров и безопасности** | `nazorat` | Плановые и внеплановые осмотры, инвентаризация, устройства, происшествия, охрана, контроль доступа на объект | `panel-nazorat.html` | 5 |
| **Бухгалтерия и риск** | `buxgalteriya` | Резерв, налог на имущество, ежемесячный отчёт в ЦБ, платежи по рассрочке и аренде, нормативные параметры | `panel-moliya.html` | 6 |
| **Администратор** | `admin` | Учётные записи, роли, справочники, интеграции, история действий. Не деловая роль | `panel.html` | 3 |

Куда перешла работа снятых ролей: руководитель филиала — это `Rahbariyat` с
привязкой учётной записи к филиалу, и такая запись видит и изменяет только
записи своего филиала; оценка и реализация — к менеджеру объектов; юрист — к
менеджеру объектов вместе с делами о взыскании; служба безопасности — к
инспектору. Сохранённая сессия или старая запись со снятым именем роли
приводится к действующей роли таблицей `ROL_YANGI` (одинаковой на клиенте и на
сервере), поэтому переход не требует ручной правки данных.

Рабочих разделов шесть (`BOLIMLAR` в `yadro/app.js`): баланс активов (реестр,
карта, приём, оформление права, архив), контроль и защита (осмотры,
инвентаризация, устройства, происшествия, охрана, доступ), стоимость и финансы
(оценка, резерв, налог, страхование), продажа и взыскание (план продаж, лоты,
предложения, договоры, аренда, дела и суд), задачи и решения, отчёты. Над ними —
стартовая панель роли, отдельно внизу — настройки.

**Простота как требование:**

- Р-0.1. В рельсе у любой роли не более шести пунктов (`ROL_YON_MAX`), один
  раздел — один пункт. Проверяется автоматически.
- Р-0.2. Любая задача роли достигается не более чем в три нажатия от её
  стартовой страницы.
- Р-0.3. Ни один экран при упрощении не удаляется. Экран, ушедший из рельса,
  остаётся доступен четырьмя путями: по ленте вкладок своего раздела, из
  поиска `Ctrl+K` (список берётся из `MKB_DARAXT`), из карточки объекта и по
  прямой ссылке. Автоматически проверяется, что каждый экран зарегистрирован в
  дереве и что вкладки ленты ведут на страницы своего раздела
  (`tests/sahifalar.test.js`). Открытый пункт: в объединённых разделах
  `nazorat` и `sotuv` лент по две, и с центра раздела вторая группа страниц по
  ленте недостижима — перечень в `docs/EKRANLAR.md`.
- Р-0.4. Отчёты собраны в один центр: в рельсе один пункт «Отчёты», отчётные
  срезы открываются карточками центра.

**Требования к разграничению:**

- Р-1. Пункт меню, недоступный роли, не отображается — не блокируется визуально,
  а отсутствует. Это относится и к боковой навигации, и к быстрым ссылкам в
  шапке, и к глобальному поиску.
- Р-2. Прямой переход по адресу в закрытый раздел не отдаёт содержимого:
  приложение заменяет страницу состоянием 403 с объяснением и путём выхода.
- Р-3. Ограничение действует на трёх уровнях: раздел (`ROL_RUXSAT`, буквы
  `o`/`y`/`t` — чтение, запись, утверждение), отдельный экран (`SAHIFA_MAXSUS`,
  `SAHIFA_AMAL`) и отдельный элемент (`data-huquq`). Панель роли открыта этой
  роли, руководству и администратору; пользователи, карточка пользователя, роли
  и интеграции — только администратору; филиалы и история действий —
  администратору и руководству; мастера приёма на баланс, редактирование записи
  объекта и заказ оценки — тем, у кого есть право записи в соответствующем
  разделе. Раздел определяется для каждой страницы, включая внутренние
  (карточки, формы, шаги мастеров): страниц оболочки без назначенного раздела
  нет, и это проверяется автоматически.
- Р-4. Отсутствующая или неизвестная роль трактуется как отсутствие доступа:
  переход на страницу входа.
- Р-5. У каждой роли есть собственная стартовая страница; наличие такой страницы
  для каждой роли проверяется автоматически.
- Р-6. Список ролей одинаков во всех источниках — оболочка, рабочее руководство,
  сервер и справочник пользователей. Расхождение считается ошибкой сборки и
  выявляется автоматически.
- Р-7. Клиентское разграничение — только представление. Сервер повторяет проверку
  на каждом запросе: без сессии — 401, недостаточные права — 403. Матрица
  «роль — разделы» на сервере состоит из тех же восьми ключей, что и на клиенте
  (`panel`, `aktivlar`, `nazorat`, `qiymat`, `sotuv`, `hisobot`, `ishlar`,
  `sozlama`); каждая коллекция данных отнесена к разделу. Совпадение клиентской
  и серверной матриц проверяется автоматически.
- Р-8. Смена роли пользователя — операция администратора и выполняется в карточке
  пользователя; каждое изменение попадает в историю действий.
- Р-9. **Четыре глаза.** Запрашивает и решает — всегда разные сотрудники. Автор
  запроса не утверждает собственный запрос, и администратор тоже: для него
  исключения нет. Правило проверяется и на клиенте, и на сервере; попытка
  утвердить свой запрос отклоняется с 403. Сокращение числа ролей правило не
  ослабляет — пары перераспределены:

  | Запрос | Кто запрашивает | Кто решает |
  |---|---|---|
  | Приём на баланс, списание с баланса | Менеджер объектов | Руководство |
  | Предложение покупателя, снижение начальной цены лота | Менеджер объектов | Руководство |
  | Отчёт об оценке | Менеджер объектов | Руководство |
  | Ставка резерва и нормативные параметры | Бухгалтерия | Руководство |
  | Доступ на объект | Инспектор | Менеджер объектов или руководство |

  Все шесть видов запросов, кроме заявки на доступ, адресованы руководству
  полем `masulRol`, и права утверждения в разделе для них недостаточно. Заявка на доступ — единственный
  поток без `masulRol`: её закрывает любой, у кого есть право утверждения в
  разделе контроля, чтобы инспектор не ждал правление ради одного пропуска.

  Проверка персональная, а не ролевая: сравниваются автор и решающий, а не их
  роли. Заместитель решает от имени утверждающего, но собственный запрос
  закрыть не может.

  До утверждения значение, зависящее от запроса, помечается как
  «ориентировочное» и в этом виде попадает в расчёты, панели и отчёты.

Роль определяет и содержание рабочего руководства: кнопка «Как работать в
системе» в шапке открывает порядок работы именно для текущей роли — цель,
последовательность шагов со ссылками на экраны, зона ответственности, сроки и
перечень закрытых для роли операций.

---

## 3. Модель данных — единый источник

Ключевое требование, определяющее достоверность системы.

### 3.1 Принцип

Все экраны читают данные из одного модуля (`malumot.js` и его расширения).
Дублирование массивов внутри страниц запрещено. Одна сущность — одна запись —
одно значение во всех разделах.

### 3.2 Сущности и связи

```
Объект (AK-ГГГГ/НННН)
├── Дело о взыскании ── история переходов между этапами
├── Приём на баланс ── акт, комиссия, дата
├── Осмотр (1:N) · Полис (1:N) · Оценка (1:N) · Документ (1:N)
├── Расход (1:N) · Происшествие (1:N) · Задача (1:N)
├── Фото и схемы (1:N) ── источник, признак общего фото
└── Контроль доступа
    ├── Точка доступа (1:N) ── Устройство (1:N) ── Работа по обслуживанию
    ├── Разрешение (1:N) ── Лицо
    ├── Заявка на доступ (1:N) ── Посещение ── Событие прохода
    ├── Инцидент (1:N)
    └── Сеанс дистанционного осмотра (1:N)

Архивная запись ── завершённый жизненный цикл объекта
```

### 3.3 Обязательные поля

**Объект:** код, тип, наименование полное и краткое, регион, адрес, площадь,
оценочная стоимость и дата оценки, состояние страхования, изображение, филиал,
этап дела и производный статус, дата приёма на баланс, основание (клиент,
договор, остаток задолженности — сведения внешнего происхождения).

**Дело о взыскании:** номер, этап, ответственный, суд, номер решения,
исполнительный документ, ближайший срок, история переходов.

**Точка доступа:** идентификатор, объект, этаж, помещение, наименование, тип
(турникет, дверь с электронным замком, шлагбаум, грузовые ворота), режим
проверки, состояние, режим работы, суточный поток, геометрия в модели здания.

**Устройство:** идентификатор, точка доступа, тип, модель, серийный номер, дата
установки, версия управляющей программы, состояние, время последней связи, заряд
батареи (для датчиков), дата следующего обслуживания.

**Разрешение:** лицо, объект, перечень точек, уровень доступа, способ проверки,
срок действия, кем выдано, состояние.

**Событие прохода:** время, объект, точка, лицо, способ, результат, причина
отказа, направление.

**Архивная запись:** код объекта, номер дела, дата приёма на баланс,
продолжительность нахождения под контролем, дата выбытия, приобретатель, сумма
выбытия.

### 3.4 Правила согласованности (проверяются автоматически)

- Д-1. Одна величина хранится один раз. Суммарные значения — результат подсчёта
  по массиву, а не записанная строка.
- Д-2. Статус объекта выводится из этапа дела; этап без назначенного статуса —
  ошибка.
- Д-3. Объект на этапе «На балансе» обязан иметь дату приёма. Этапы
  «Конфисковано» и «На балансе» невозможны без исполнительного документа.
- Д-4. Проценты в круговых срезах в сумме дают 100; количества по разрезам —
  регионам, филиалам, состояниям — совпадают с общим числом объектов.
- Д-5. Оценочная стоимость и количество дней просрочки положительны.
- Д-6. Вторичные списки — осмотры, полисы, оценки, документы, происшествия,
  расходы, задачи, события контроля доступа — ссылаются на объект
  **идентификатором**, а не текстом. Отображаемое имя, краткое имя, тип и регион
  выдаёт реестр `OBYEKT_INDEKS`; хранить имя объекта второй раз запрещено.
- Д-7. Точка доступа принадлежит существующему объекту и существующему этажу
  модели здания; устройство — существующей точке; событие, разрешение и
  посещение — существующему лицу и существующей точке. Записей без владельца
  нет.
- Д-8. У каждого объекта со зданием есть хотя бы одна точка доступа; у
  транспорта, техники и оборудования точек доступа нет.
- Д-9. Индекс контроля нигде не хранится — он вычисляется из текущих данных при
  каждом обращении.
- Д-10. Фото объекта — только фактический файл (локальный реестр или
  загрузка пользователя). Подставных изображений и чертежей нет: без фото
  показывается пустое состояние.

Исключение из Д-1 — история действий: её формулировки фиксируют состояние на
момент действия и намеренно не пересчитываются при последующих изменениях.

### 3.5 Реестр объектов

Правило Д-6 обеспечивается реестром `OBYEKT_INDEKS`. Он собирается из активных
записей и архива и выдаёт по коду объекта его полное имя, краткое имя, тип,
регион и изображение.

```
obyekt(id)             -> запись реестра или null
obyektNomi(id, кратко?) -> отображаемое имя
obyektHududi(id, ...)  -> регион
joyNomi(id, внутри, разделитель?) -> «Navruz Plaza, 1-qavat»
```

Разорванная ссылка видна сразу: вместо имени выводится код с префиксом `?`, а
автоматическая сверка `moslikTekshiruvi()` возвращает такую запись как ошибку.

---

## 4. Индекс контроля

Индекс контроля — единственный числовой показатель, отвечающий на вопрос
«насколько объект в порядке». Значение от 0 до 100 %, вычисляется в
`malumot-indeks.js` из пяти проверок с весами:

| Составляющая | Вес | Норма | Что снижает |
|---|---|---|---|
| Актуальность осмотра | 25 | Недвижимость — раз в 90 дней, транспорт — раз в 30, земля — раз в 180 | Просроченный плановый осмотр; отсутствие проведённых осмотров |
| Страховая защита | 25 | Полис действует, до окончания более 30 дней, покрытие не ниже оценочной стоимости | Покрытие ниже оценки; менее 30 дней до окончания; истёкший полис; отсутствие полиса |
| Актуальность оценки | 20 | Переоценка не реже одного раза в 12 месяцев | Возраст оценки сверх норматива; отсутствие оценки |
| Комплектность документов | 15 | Технический паспорт, кадастровый документ, акт приёма | Каждый недостающий документ |
| Контроль доступа | 15 | Все точки доступа в сети | Точки не в сети; каждый незакрытый инцидент |

Сумма весов равна 100 — это проверяется автоматически. Балл каждой составляющей
снижается ступенчато: в пределах норматива — полный вес, до полутора нормативов —
0,6, до двух — 0,3, свыше — 0.

Итоговое значение делится на четыре уровня:

| Значение | Уровень |
|---|---|
| 85 — 100 | Высокий контроль |
| 70 — 84 | Устойчивый |
| 50 — 69 | Требует внимания |
| ниже 50 | Критический |

Требования:

- И-1. Индекс показывается в карточке объекта, в реестре, в панелях ролей и в
  разрезах отчётов — везде одно и то же число.
- И-2. Карточка объекта раскрывает состав индекса: по каждой составляющей —
  фактическое значение, норматив, состояние и причина снижения.
- И-3. Слабые составляющие сортируются от худшей к лучшей и служат готовым
  перечнем действий.
- И-4. Средний индекс по филиалу и распределение объектов по уровням считаются
  из тех же значений.

---

## 5. Модули и функциональные требования

Всего 101 экран. Ниже группы описаны по назначению; группировка функциональная
и не совпадает один в один с шестью разделами рельса. Пофайловый перечень —
`docs/EKRANLAR.md`.

Навигация плоская. Боковая навигация показывает один пункт на раздел, не более
шести пунктов на роль; остальные экраны раздела открываются из центра раздела
(карточка «Разделы» на его главной странице), из карточки объекта, из поиска
`Ctrl+K` и по прямой ссылке. У каждого раздела есть единая лента вкладок,
одинаковая на всех его страницах; карточки, формы и шаги мастеров открываются из
списков и лент. Вкладка, закрытая роли, в ленте не показывается.

### 5.0 Вход, служебные состояния и презентация (7 экранов)

Вход в систему с выбором рабочего места, восстановление пароля, установка нового
пароля, состояние 403 «нет прав», состояние 404 «страница не найдена», точка
входа, направляющая пользователя на стартовую страницу его роли, и презентация
проекта (раздел 5.11).

### 5.1 Панель роли (4 экрана)

Собственное рабочее место для каждой рабочей роли: `panel.html` — руководство и
администратор, `panel-obyekt.html` — менеджер объектов, `panel-nazorat.html` —
инспектор осмотров и безопасности, `panel-moliya.html` — бухгалтерия и риск.
Панель показывает срочное на сегодня, сроки и собственные запросы, ожидающие
решения. Показатели считаются из данных, ничего вне прав роли на панель не
попадает. Объекты, требующие внимания, выводятся в
порядке возрастания индекса контроля — худший сверху.

Главная панель содержит блок «Имущество на балансе»: стоимость имущества на
балансе и число объектов, график по годам «принято на баланс / реализовано»,
разрез по регионам и охват контролем — четыре кольца (осмотр, страхование,
оценка, контроль доступа), каждое ведёт в свой раздел.

### 5.2 Реестр объектов, карта и архив (17 экранов)

Центральный реестр с поиском, фильтрами и сортировкой; два представления —
карточки и таблица. Карточка объекта состоит из десяти вкладок: общие сведения,
фото, финансы, документы, осмотры, расходы, коммунальные услуги, защита,
продажа, история.
Отдельно — редактирование записи и официальная печатная форма паспорта объекта
АН-01. Документы объекта ведутся в карточке: отдельного раздела документов нет,
документ привязывается к объекту идентификатором.

**Приём на баланс** — мастер из трёх шагов: основание и объект; документы и
состав комиссии; акт и подтверждение. До подтверждения объект в реестр не
попадает. После подтверждения ставится задача на первый осмотр и оформление
страхования.

**Карта** — региональная карта с панелью регионов; координаты берутся из записей
объектов. Это второе представление того же реестра, вкладка «На карте».

**Архив** — постоянная запись только для чтения о завершённом жизненном цикле:
объект, номер дела, дата приёма на баланс, продолжительность нахождения под
контролем, дата выбытия, приобретатель и сумма выбытия. Реестр архива и лист
архивной записи; архивная запись не редактируется.

### 5.3 Электронный контроль доступа (17 экранов)

Ключевой модуль наблюдения. Отвечает на вопрос, кто и на каком основании
находится на объекте банка.

**Пост оператора.** Очередь заявок на доступ, перечень находящихся на объекте
прямо сейчас, поток последних событий, состояние точек. Рабочий экран дежурного.

**Точки доступа.** Реестр и карточка. Тип: турникет, дверь с электронным замком,
шлагбаум, грузовые ворота. Режим проверки: распознавание лица и карта, карта,
карта и PIN, распознавание лица, дистанционное открытие. Состояние: в сети, не в
сети, на обслуживании. Хранится режим работы, суточный поток и привязка к этажу
и помещению модели здания — точка на плане и точка в реестре это один объект.

**Журнал событий и карточка события.** Время, объект, точка, лицо, способ
проверки, направление и результат: разрешено, отказано, предупреждение. Отказ
всегда сопровождается причиной: срок разрешения истёк, карта не в списке, лицо
не совпало, попытка вне рабочего времени, неверный PIN, объект опечатан.
Карточка события содержит решение оператора.

**Заявка на доступ.** Очередь заявок и форма новой. Цель, дата, время,
продолжительность, заявитель, утверждающий. Состояния: ожидает, утверждена,
исполнена, отклонена; отклонение — с указанием причины.

**Реестр разрешений.** Действующие права доступа: лицо, объект, перечень точек,
уровень (полный доступ, ограниченный доступ, в сопровождении), способ проверки,
срок действия, кем выдано. Состояния: действует, срок истёк, приостановлено.

**Посещения и живой контроль посещения.** Реестр посещений с временем входа,
выхода и продолжительностью. Экран живого посещения ведёт визит по шагам:
подтверждение личности, открытие точки, счётчик времени на объекте, журнал
активности, управление дверью.

**Выходная проверка.** Акт выхода по описи: проверяются помещения, оборудование
и инвентарь; результат фиксируется до закрытия посещения.

**Дистанционный осмотр.** Сеанс из шести стадий: связь установлена, точка
доступа открыта, помещения обойдены, фото и видео записаны, двери закрыты,
протокол подписан. Хранится число задействованных камер, длительность и
состояние: запланирован, идёт, завершён.

**Устройства.** Реестр и карточка. Семь типов: электромагнитный замок,
считыватель карт, терминал распознавания лица, видеокамера, контроллер доступа,
видеодомофон, датчик положения двери. Модель, серийный номер, дата установки,
версия управляющей программы, время последней связи, заряд батареи, дата
следующего обслуживания. Состояния: работает, предупреждение, неисправно.
Карточка содержит диагностику, журнал и историю обслуживания.

**Мастер установки устройства** — четыре шага: размещение, устройство,
подключение, активация. Проверяется уникальность серийного номера в реестре и
формат сетевого адреса; при ошибке шаг не завершается.

**Задание на обслуживание.** Вид работы — профилактика, устранение
неисправности, обновление управляющей программы, замена батареи, перенастройка,
установка. Устройство, точка, исполнитель, запасные части, состояние.

**Доска инцидентов и карточка инцидента.** Пять колонок: новое, проверяется,
устраняется, решено, закрыто. Типы: попытка несанкционированного прохода, дверь
осталась открытой, несовпадение при распознавании лица, проход вне рабочего
времени, потеря связи с устройством, нарушена пломба, пропал сигнал камеры,
низкий заряд батареи датчика. Уровни значимости: высокий, средний, низкий.
Закрытие возможно только с заключением и указанием принятой меры.

Отчёт по контролю доступа за период — проходы по дням, использованные способы
проверки, активность по объектам, причины отказов — находится в разделе отчётов
(5.7).

### 5.4 Контроль осмотров (6 экранов)

План осмотров с регламентной периодичностью по типу имущества, просроченные,
история, назначение осмотра, проведение осмотра с листом проверки и
фотофиксацией, акт осмотра. Акт печатается прямо с листа акта средствами
браузера. Акт не считается оформленным без заполненного листа проверки, оценки
состояния и подписи. Просроченный осмотр отдельно помечается и снижает индекс
контроля.

### 5.5 Оценка и страхование (7 экранов)

**Оценка.** Реестр отчётов об оценке, заказ на оценку, ввод результата, лист
отчёта. Введённая стоимость становится значением карточки объекта — второго
места хранения нет. Резкое снижение стоимости — основание для открытия
происшествия.

**Страхование.** Полисы, лист полиса, форма продления. Выгодоприобретатель —
банк, покрытие не ниже оценочной стоимости, продление начинается за 30 дней до
окончания. Разрыв страховой защиты снижает индекс контроля и виден в панели
инспектора.

### 5.6 Взыскание и суд (6 экранов)

Доска дел по шести этапам со счётчиками и суммами, лист дела с историей,
подготовка иска, календарь судебных заседаний, лист заседания с протоколом, ввод
решения суда. Перевод дела на следующий этап возможен только по порядку и только
с записью в историю.

### 5.7 Отчёты (9 экранов)

Единый центр отчётности `hisobotlar.html` и восемь готовых срезов: активы на
балансе, регионы, расходы на содержание, индекс контроля, взыскание и суд,
контроль доступа, ежемесячный отчёт в ЦБ, выгрузка данных. В рельсе — один
пункт: срезы открываются карточками центра, каждая карточка подписана тем, что в
ней есть. Каждый срез считается из общего модуля данных; сумма по срезу сходится
с общим итогом.

### 5.8 Задачи (3 экрана)

Задачи с отметкой выполнения и добавлением, очередь утверждений, уведомления с
состоянием прочтения.

### 5.9 Настройки (8 экранов)

Профиль, пользователи, карточка пользователя, матрица ролей и разделов, филиалы,
интеграции, история действий, рабочее руководство.

### 5.10 Фото объекта

Вкладка «Suratlar» (`obyekt-suratlar.html`) показывает все фото и схемы объекта
из `rasmlar[]`: сетка миниатюр, просмотр во весь экран с листанием клавишами
← →, добавление фото с указанием источника и выбор основного фото. Фото
хранятся через `MKBapi.fayl`: в серверном режиме — на сервере банка, без
сервера — в браузере.

В демонстрационном режиме фото нет, вместо них — значок вида объекта и надпись
«Surat bank tarmog'ida saqlanadi». В локальном режиме у объекта без фото —
«Surat yuklanmagan» и кнопка «Surat qo'shish».

Прежние трёхмерный навигатор, план этажа (`obyekt-3d.html`,
`obyekt-qavat.html`, `yadro/bino.js`, `yadro/qavat.js`, `yadro/nav3d.js`) и
изометрические чертежи (`yadro/obyekt-rasm.js`, `assets/obyekt/`) изображали
условную геометрию и из системы удалены.

### 5.11 Презентация проекта

Экран `taqdimot.html` — интерактивная презентация проекта для руководства банка.
Открытая страница: ссылка на неё стоит внизу боковой навигации и на главной
панели. Десять сцен: обложка, текущее положение, решение, варианты решения для
объектов без электроснабжения, калькулятор расхода энергии, распознавание лица и
пожарная защита, уровни защиты с калькулятором сметы, экраны системы, план
внедрения, решение.

- ПР-1. Показатели презентации берутся из общего модуля данных и совпадают с
  главной панелью; собственных копий чисел презентация не хранит.
- ПР-2. Значения, введённые в калькуляторы, сохраняются только в браузере
  пользователя (`localStorage`, ключ `mkb-taqdimot`) и никуда не передаются.
- ПР-3. Смета выгружается в CSV.
- ПР-4. Презентацию можно открыть с нужной сцены (`?sahna=N`) или показать одну
  сцену отдельно (`?faqat=N`).
- ПР-5. Презентация переводится тем же словарём, что и остальные экраны.

### 5.12 Печатные формы, импорт и работа инспектора в поле

**Единый шаблон печати.** Семь форм печатаются из одного модуля
(`yadro/dalolatnoma.js`, `yadro/dalolatnoma.css`): акт осмотра, акт приёма-передачи
на баланс, акт инвентаризации, паспорт объекта, акт списания с баланса, акт
приёма-передачи покупателю и акт о происшествии. Лист — A4: номер, дата и место;
пункты в виде таблицы; фототаблица 3×2 с временем съёмки и расстоянием до объекта;
состав комиссии со строками для подписи; код проверки (первые 12 символов SHA-256
от содержания записи) и QR. Кнопки, хлебные крошки, общий заголовок оболочки и
адрес страницы на бумагу не выводятся; номер листа и код стоят в поле `@page`.
Акт осмотра на 8 пунктов с шестью фото занимает один лист.

Акт выходит с водяным знаком «Qoralama» (черновик) и причиной в шапке, если не
выполнено условие его силы:

| Форма | Условие силы |
|---|---|
| Акт осмотра | фото не меньше нормы `korikSuratBino` / `korikSuratTransport` и не меньше числа пунктов с недостатками |
| Акт приёма на баланс | фототаблица не меньше той же нормы |
| Акт инвентаризации | подсчёт завершён |
| Акт списания с баланса | заявку утвердил другой сотрудник (правило четырёх глаз) |
| Акт передачи покупателю | договор оплачен полностью или наложен запрет на отчуждение |
| Акт о происшествии | происшествие решено или закрыто |

QR открывает страницу формы с параметром `kod`. Страница пересчитывает код из
текущей записи и сообщает «код совпадает» или «код не совпадает»; текущий код при
несовпадении не показывается, чтобы его нельзя было переписать на поддельный лист.
Код не зависит от языка интерфейса и времени печати.

**Импорт из Excel и CSV.** Пять реестров принимают файл `.xlsx` (первый лист) или
CSV: активы, страховые полисы, результаты оценки, план осмотров, устройства.
Кнопка «Namuna fayl» выдаёт шаблон с нужными столбцами и вторым листом правил.
Перед записью каждая строка проверяется теми же правилами, что и ручная форма;
строка с ошибкой не записывается и возвращается в CSV отклонённых строк. Импорт
не обходит утверждение: актив входит со статусом «Оформляется», а не «На балансе»;
результат оценки создаёт заявку `baho` на утверждение руководством, и стоимость
актива меняется только после решения другого сотрудника. Чтение `.xlsx` выполнено
без сторонних библиотек (`yadro/jadval-fayl.js`, распаковка средствами браузера).

**Инспектор в поле.** Форма осмотра на телефоне открывает камеру напрямую
(`capture="environment"`). Каждое фото получает штамп: время загрузки, время съёмки
из EXIF, координаты (из EXIF или геолокации телефона) и расстояние до точки из
реестра; фото старше суток и снятые дальше 500 м от объекта подсвечиваются.
Штампы и время осмотра записываются в акт. Черновик формы и штампы хранятся на
устройстве до подписи. «Маршрут на сегодня» выстраивает сегодняшние и просроченные
осмотры инспектора: от самого просроченного, дальше каждый раз к ближайшему объекту;
при координате по центру района расстояние помечено «≈».

---

## 6. Требования к интерфейсу

### 6.1 Фирменный стиль

Тёплая градиентная сцена (персиковый `#F7EDE9`, лавандовый `#EEEFF7`, мятный
`#EAF3EC`), белые скруглённые карточки (радиус 28 px, внутренние плитки 22 px),
чернильный акцент `#0B0B0A` в виде пилюли для основных действий и активной
навигации. Сигнальные цвета отделены от акцента: мятный, розовый, лавандовый,
песочный. Все значения — токены в `:root` файла `yadro/app.css`; при получении
брендбука банка заменяются значения токенов, разметка не трогается.

Шрифты Outfit и Manrope; кириллица обеспечивается Manrope. Значки — единый
спрайт из 86 символов, подключаемый один раз на страницу.

Диаграммы строятся только собственным построителем `yadro/chizma.js`: область со
сглаженной кривой и градиентом, столбцы, сгруппированные столбцы для сравнения
рядов, кольцевая диаграмма с итогом в центре, мини-график для плиток показателей. Полосы и доли, собранные из блоков разметки,
не допускаются.

### 6.2 Обязательные нормы

- Т-1. Контраст основного текста не ниже 4.5:1, крупного — 3:1 (WCAG AA).
- Т-2. Минимальная область нажатия — 32×32 px, для основных действий — 40 px и выше.
- Т-3. Горизонтальная прокрутка страницы недопустима на ширинах 1280–1920 px;
  широкие таблицы прокручиваются внутри своего контейнера. Ширины 390, 768,
  1280 и 1440 px проверяются сценарием `tests/olcham.mjs`.
- Т-4. Цвет не может быть единственным носителем смысла: состояние дублируется
  подписью.
- Т-5. Видимый фокус на всех интерактивных элементах.
- Т-6. Каждый интерактивный элемент даёт отклик: переход, изменение состояния или
  уведомление. Элементов без реакции нет.
- Т-7. Вложенные интерактивные элементы одного типа запрещены.
- Т-8. Треки сетки, содержащие таблицы, объявляются как `minmax(0, …)`.
- Т-9. Оболочка страницы одна: боковая навигация и шапка рисуются из общего
  модуля, страница отвечает только за содержание.
- Т-10. Единая пластика на всех экранах — разнобой зрелости недопустим.

### 6.3 Двуязычность

Узбекский — исходный язык, русский — через словарь `tarjima.js` (3060 ключей и 89
правил подстановки). Ключ словаря — текст интерфейса, поэтому перевод не требует
разметки в HTML. Переводятся также подписи полей, всплывающие подсказки,
альтернативные тексты и заголовок вкладки. Переключение происходит без
перезагрузки, выбор сохраняется между сеансами. Имена собственные, адреса, номера
документов и даты не переводятся. Новый текст интерфейса добавляется в словарь
одновременно с вёрсткой.

---

## 7. Требования к содержанию

- С-1. Данные правдоподобны для узбекского банка: реальные районы, типовая
  адресация, суммы в сумах в реалистичных диапазонах. Филиалы названы так, как
  принято в банке, — в форме БХМ/БХО; регионов четырнадцать, их количества в
  сумме дают итог портфеля.
- С-2. Терминология соответствует практике: исполнительный документ, орган
  исполнения, претензия, ипотека, оборотные средства, кадастровый документ. Для
  объекта на этапе «На балансе» используются балансовые термины — «балансовая
  стоимость», «на балансе N дней», — а не термины задолженности и просрочки.
- С-3. В интерфейсе нет служебных пометок незавершённости и условного текста
  вместо содержания.
- С-4. Не используются эмодзи, палитры по умолчанию из внешних библиотек,
  служебные пути и названия средств разработки — ни в коде, ни в документации.
- С-5. Изображения не повторяются бессмысленно: каждая фотофиксация осмотра несёт
  объект, дату и автора.
- С-6. Наименование объекта, региона и филиала в списках выбора берётся из
  реестра, а не перечисляется в разметке.

### 7.1 Фактический реестр имущества

Поставка содержит только условные демонстрационные данные. Фактический перечень
имущества на балансе банка — конфиденциальные сведения, и работа с ним
подчиняется отдельным требованиям:

- К-1. Фактический реестр загружается на рабочий компьютер командой
  `python vositalar/import_taqdimot.py <презентация.pptx>` и хранится только в
  каталоге `mahalliy/` (`obyektlar.json` и фотографии `rasm/*.jpg`).
- К-2. Каталог `mahalliy/` исключён из репозитория (`.gitignore`) и не входит ни
  в поставку, ни в публикуемую копию системы.
- К-3. Слой данных читает фактический реестр только при открытии системы с
  локального адреса (`localhost`, `127.0.0.1`). На любом другом адресе запрос
  к нему не выполняется.
- К-4. Фактические данные накладываются на записи реестра без изменения
  идентификаторов и связей; правила согласованности Д-1…Д-10 продолжают
  выполняться.
- К-5. Источник данных виден пользователю: индикатор внизу боковой навигации
  показывает «Mahalliy reyestr». Щелчок по индикатору возвращает
  демонстрационные данные (выбор хранится в `localStorage`, ключ `mkb-manba`);
  удаление каталога `mahalliy/` отключает фактический реестр полностью.
- К-6. Автоматические проверки и засев хранилищ сервера выполняются только на
  демонстрационных данных.

Порядок работы и гарантии описаны в `docs/ARXITEKTURA.md`, раздел 10.

---

## 8. Критерии приёмки

| № | Проверка | Норма |
|---|---|---|
| П-1 | Все экраны отвечают, оболочка на месте | 101 из 101 |
| П-2 | Ошибки в консоли браузера | 0 |
| П-3 | Битые внутренние ссылки и изображения; ссылки на экраны вне состава поставки | 0 |
| П-4 | Неопределённые токены оформления | 0 |
| П-5 | Горизонтальная прокрутка на 1280/1440/1600/1920 | 0 экранов |
| П-6 | Интерактивные элементы без реакции | 0 |
| П-7 | Элементы меньше 32 px | 0 |
| П-8 | Закрытые для роли маршруты, отдавшие содержимое | 0 |
| П-17 | Пунктов рельса у одной роли | не более 6 |
| П-18 | Экраны, ушедшие из рельса и ставшие недостижимыми | 0 |
| П-19 | Запросы, утверждённые собственным автором | 0 |
| П-9 | Расхождения по правилам согласованности данных (Д-1…Д-10) | 0 |
| П-10 | Разорванные ссылки на объект во вторичных списках | 0 |
| П-11 | Объекты без точки доступа | 0 |
| П-12 | Сумма весов индекса контроля | 100 |
| П-13 | Непереведённый текст интерфейса в русском режиме, по каждой роли отдельно | 0 |
| П-14 | Контраст ниже AA | 0 |
| П-15 | Отказы автоматических проверок, все наборы `tests/*.test.js` | 0 |
| П-16 | Фактические данные банка в репозитории и в публикуемой копии | 0 |
| П-20 | Пересмотр прав: заблокированные учётные записи с закреплёнными активами; акт «Права пересмотрены» за квартал | 0; акт подписан руководством |
| П-21 | Экран состояния системы у администратора: режим, давность каждой коллекции, возраст сигналов устройств, состояние 14 интеграций, версия | все пункты на одном экране |
| П-22 | Сверка реестра с АБС: остаток счёта 16701 и сумма балансовой стоимости по реестру | расхождение 0 или расписано по активам; отчёт ЦБ не отмечается «сдан» без отметки о сверке |
| П-23 | Печатные формы: семь форм из единого шаблона, A4, без элементов интерфейса и адреса страницы; номер, дата, место, комиссия, код проверки и QR; «Qoralama» при невыполненном условии силы (раздел 5.12) | 7 из 7; акт осмотра на 8 пунктов и 6 фото — 1 лист; QR с изменённой записью даёт «код не совпадает» |
| П-24 | Импорт: строки с ошибками, записанные в реестр; активы, вошедшие со статусом «На балансе», и оценки, изменившие стоимость без утверждения | 0 |

Проверка П-6 выполняется реальным нажатием и фиксацией отклика, а не наличием
атрибута. Проверки П-9 — П-12 выполняются автоматически: сверкой
`moslikTekshiruvi()` и наборами проверок из `tests/`. Проверка П-3
автоматизирована набором `tests/sahifalar.test.js`. Проверка П-8 выполняется
двумя способами: разбором матрицы доступа и заходом под каждой ролью.

---

## 9. Интеграции и следующий этап

### 9.1 Что уже работает и в следующий этап не входит

- **Уведомления внутри системы:** колокольчик, движок правил сроков (осмотры,
  полисы, оценка, резерв, судебные заседания), задачи с эскалацией, отключение
  правил сотрудником для себя.
- **Реализация:** лоты, предложения, комиссия, договоры, графики платежей, аренда,
  снятие запрета, выбытие с баланса через утверждение другим сотрудником.
- **История изменений:** каждая запись через `MKBapi` попадает в журнал действий
  (`amallar-tarixi.html`) с автором, временем и значениями «было → стало».
- **Рабочее место инспектора в браузере телефона:** маршрут на день, лист проверки,
  съёмка камерой со штампом времени и координат, черновик на устройстве (раздел 5.12).
- **Печать актов** из единого шаблона с кодом проверки и QR (раздел 5.12).

### 9.2 Внешние системы

На дату редакции ни одна из 14 систем не подключена. Данные, которые они будут
передавать, сейчас вносятся вручную, и такая запись помечается как ручная.
Состояние подключения хранится в `PARAMETRLAR`/`INTEGRATSIYALAR`, а не в коде
страниц, и показывается на экране `integratsiyalar.html`.

| № | Система | Направление и данные | Протокол | Что нужно для подключения |
|---|---|---|---|---|
| 1 | АБС банка | ← остатки счёта 16701, договоры и платежи покупателей; → проводки по выбытию | REST или файловый обмен по расписанию | спецификация API АБС, служебная учётная запись, канал в сегменте АБС |
| 2 | E-auksion | ← лоты, ставки, протокол торгов, победитель | API оператора площадки | договор с оператором, ключ доступа, сертификат организации |
| 3 | Кадастр (davreestr) | ← сведения об объекте, правах и запретах по кадастровому номеру | межведомственный запрос | соглашение об обмене, доступ через my.gov.uz или прямой канал |
| 4 | YHXX (ГАИ) | ← регистрация транспортного средства, запреты | межведомственный запрос | соглашение об обмене с МВД |
| 5 | MIB (исполнение) | ← движение исполнительных документов по делам взыскания | межведомственный запрос | соглашение с Бюро принудительного исполнения |
| 6 | Налоговый комитет | ← кадастровая стоимость и налог на имущество; → сведения о выбытии | API или межведомственный запрос | соглашение об обмене, ИНН банка как отправителя |
| 7 | Портал отчётности ЦБ | → отчёт о непрофильных активах | формат и канал регулятора | утверждённый формат выгрузки, учётная запись банка на портале |
| 8 | my.gov.uz (межведомственные запросы) | ← ответы госорганов по объекту и собственнику | шина межведомственного взаимодействия | подключение банка к шине, перечень разрешённых запросов |
| 9 | E-IMZO | подпись актов и решений ЭЦП сотрудника | локальный модуль E-IMZO в браузере | ключи ЭЦП у членов комиссии, установка модуля на рабочие места |
| 10 | Active Directory | вход по учётной записи домена, группы → роли | LDAPS или SAML | служебная учётная запись, соответствие групп AD ролям системы |
| 11 | SMS-шлюз | → напоминания о сроках, коды входа | HTTP API оператора | договор с оператором, отправитель (альфа-имя) |
| 12 | Telegram-бот | → уведомления сотрудникам, приём подтверждений | Bot API через прокси банка | бот, разрешение ИБ на исходящий канал, привязка сотрудников |
| 13 | Пульт МВД «Qo'riqlash» | ← тревоги с объектов под пультовой охраной | протокол пульта (Contact ID через шлюз) | договор с охраной, шлюз на стороне пульта |
| 14 | NVR/IoT-шлюз | ← события датчиков и камер, состояние устройств; → команды замкам | MQTT (TLS) и ONVIF внутри сети банка | шлюз в сегменте банка, сертификаты устройств, разрешение ИБ |

Демонстрационные подключения к открытым источникам (прогноз погоды, тестовый
MQTT-брокер, трансляции городских камер с разрешением на встраивание) помечены как
публичные образцы и не являются подключением объектов банка.

### 9.3 Следующий этап

- **Работа без связи.** Манифест и service worker: страницы осмотра,
  инвентаризации и актов открываются без сети, записи ставятся в очередь и
  отправляются при появлении связи. Сейчас без сети сохраняется только черновик
  формы на устройстве.
- **Доставка уведомлений** вне системы: почта, SMS, Telegram (системы 11 и 12).
- **Неизменяемое хранилище журнала** на сервере: только добавление, с цепочкой
  хешей, как у кода проверки актов.
- **Подпись актов E-IMZO** (система 9) вместо бумажной подписи.
- **Расширение модели здания.** Загрузка реальных обмеров и планов, привязка
  устройств к точным координатам. Сейчас у большинства объектов координата стоит
  по центру района, поэтому расстояния в маршруте инспектора приблизительные.

Требуют подтверждения заказчиком: нормативная периодичность осмотров и
переоценки по типам имущества, норма фото для акта осмотра, перечень обязательных
документов объекта, пороги уровней индекса контроля, состав ответственных
подразделений и справочник филиалов.
